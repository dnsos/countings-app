<div class="px-5">
  <h1 class="font-bold text-4xl"><%= t("app.title") %></h1>
  <p class="mt-4"><%= t("home.index.intro") %></p>
  <div class="mt-7 flex flex-wrap gap-3">
    <%= link_to t("activerecord.models.counting.other").capitalize, countings_path, class: 'primary inline-block' %>
    <% unless user_signed_in? %>
      <%= link_to t("devise.sessions.new.sign_in").capitalize, new_user_session_path, class: 'secondary inline-block' %>
    <% end %>
  </div>
</div>
