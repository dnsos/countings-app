<% content_for(:html_title) { @counting.title } %>

<% content_for(:header_slot) {
    render ButtonComponent.new(scheme: :link, tag: :a, href: countings_path, class: 'p-0') do
      "← #{t('countings.index.title.default')}"
    end
  }
%>

<% content_for(:main_tag_classes) { 'p-5' } %>

<%= render @counting %>
