<header class="w-full h-14 px-5 py-1 flex justify-between border-b border-b-blue-700 z-20">
  <div class="flex items-center text-white font-medium">
    <%= content_for?(:page_title) ? yield(:page_title) : t('app.title') %>
  </div>
  <%= render 'shared/navbar' %>
</header>