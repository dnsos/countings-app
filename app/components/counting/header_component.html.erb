<%= content_tag :div, **@args do -%>
  <h1><%= @title %></h1>
  <% if ongoing? %>
    <div class="order-first">
      <h2
        class="inline-block px-2 py-1 text-xs text-blue-300 bg-blue-800 border border-blue-400 rounded-md mb-3"
      >
        <%= @remaining_time %>
      </h2>  
    </div>
  <% end %>
  <p class="mt-3 text-sm text-blue-300">
    <time><%= I18n.l(@starts_at, format: :long) %></time>
    –
    <time><%= I18n.l(@ends_at, format: :long) %></time>
  </p>
<% end -%>
